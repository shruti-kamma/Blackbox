// The id app/ranking/layout.tsx sets on its wrapper div, so
// ranking-theme.css can scope the /ranking section's fonts/colors to
// #ranking-root without leaking them into the rest of job-portal.
export const RANKING_ROOT_ID = "ranking-root";
