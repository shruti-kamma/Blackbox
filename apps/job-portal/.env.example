DATABASE_URL="postgresql://blackbox:blackbox@localhost:5432/blackbox?schema=public"
REDIS_URL="redis://localhost:6379/0"
AUTH_SECRET="replace-with-a-long-random-string"

# Free, no billing account required — sign up at https://console.groq.com,
# used for the AI interview feature (question generation + answer
# evaluation). Currently the active provider, see src/lib/ai-interview/index.ts.
GROQ_API_KEY=

# Alternative provider (src/lib/ai-interview/gemini-provider.ts), not
# currently active — required billing on the account this was tested with.
GEMINI_API_KEY=
