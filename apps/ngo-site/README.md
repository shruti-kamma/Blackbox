# NGO Site

Deferred. Third site in the Blackbox suite — not being actively developed yet.

Will be scaffolded the same way as `apps/job-portal` and `apps/rankings` (Next.js + TypeScript + Tailwind) when prioritized.
